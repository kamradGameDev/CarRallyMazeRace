using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurrentPosCenterRoad : MonoBehaviour
{
    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.tag == "Road")
    //    {
    //        if (MovementCar.instance.moveCurrentState == MoveCurrentState.moveMinusY || MovementCar.instance.moveCurrentState == MoveCurrentState.movePlusY)
    //        {
    //            MovementCar.instance.car.position = new Vector2(collision.transform.position.x, this.transform.position.y);
    //        }
    //        else if (MovementCar.instance.moveCurrentState == MoveCurrentState.moveMinusX || MovementCar.instance.moveCurrentState == MoveCurrentState.moveMinusY)
    //        {
    //            MovementCar.instance.car.position = new Vector2(this.transform.position.x, collision.transform.position.y);
    //        }
    //        //MovementCar.instance.car.position = collision.transform.position;
    //    }
    //}
}
